$( function() {
  $( "#cassette" ).draggable({
    //axis: "y",
    disabled: false,
    cursor: "move",
    start: function( event, ui ) {

    },
    stop: function( event, ui ) {

    }
  });
} );
